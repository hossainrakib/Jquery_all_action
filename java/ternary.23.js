// ternary and if else condition use is same ;


/* var number = Number(prompt('enter your number  : '));

// if( number > 0)
// console.log('positive');
// else if(number < 0)
// console.log('nagetive');
// else
// console.log('zero'); 

// var result = number > 0 ? 'positive': 'nagetive';

 //var result = number > 0 ? 'positive': number < 0 ? 'nagetive' : 'zero';


document.write(result); */

// or simple use
/* 
var number = Number(prompt('enter your number  : '));

number > 0 ? console.log('positove') : number < 0 ? console.log('nagetive') : console.log('zero'); */
 
